# Tasks: Enhanced Worktree Listing

- [x] **Refactor Table Layout**: Swap columns and move indicator to the left.
- [x] **Implement Dirty Detection**: Call `hasUncommittedChanges` for each worktree in the list.
- [x] **Update Labels**: Use `primary` and `dirty` in the TYPE column.
- [x] **Verify**: Run `wts list` and ensure outputs match expectations.
